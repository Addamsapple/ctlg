#include <memory>

#include "command.h"
/*
class EditType : public UndoableCommand {
	private:
		size_t _position;
		std::unique_ptr<Field> _type;
		std::unique_ptr<FieldConstructorInterface> _constructor;
		std::vector<std::unique_ptr<Field>> _fields;
		
		void _swap();

	OVERRIDE_UNDOABLE_MEMBER_FUNCTIONS
};*/
